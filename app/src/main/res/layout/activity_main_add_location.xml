<?xml version="1.0" encoding="utf-8"?>
<layout>

    <data>

        <variable
            name="activity"
            type="wear.weather.main.ui.MainAddLocationActivity" />
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <androidx.appcompat.widget.Toolbar
            android:id="@+id/toolbar_add_location"
            android:layout_width="match_parent"
            android:layout_height="@dimen/toolbar_height"
            app:layout_constraintTop_toTopOf="parent">

            <ImageButton
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="start"
                android:background="#ffffff"
                android:src="@drawable/ic_toolbar_back" />

            <TextView
                android:id="@+id/tv_add_location"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="start"
                android:layout_marginLeft="35dp"
                android:lineHeight="24sp"
                android:text="@string/addLocation"
                android:textColor="#00060c"
                android:textSize="18sp" />
        </androidx.appcompat.widget.Toolbar>

        <AutoCompleteTextView
            android:id="@+id/auto_complete_tv_search"
            android:layout_width="match_parent"
            android:layout_height="50dp"
            android:layout_marginTop="20dp"
            android:completionHint="검색어를 입력하세요"
            android:completionThreshold="1"
            android:textColorHint="#555555"
            app:layout_constraintTop_toBottomOf="@+id/toolbar_add_location" />

        <TextView
            android:id="@+id/tv"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="90dp"
            android:text="테스트 데이터"
            app:layout_constraintTop_toBottomOf="@id/auto_complete_tv_search" />

    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>